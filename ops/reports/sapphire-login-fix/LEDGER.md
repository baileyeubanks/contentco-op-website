# CURSOR-BLIND-②-CCO — Sapphire `/os` login

**2026-08-03** — Replaced dark neon ROOT login with Sapphire Light UI (`os-login-*`), CCO OS copy, `role="alert"` on auth error, and `root-*`→`os-*` localStorage migration (read legacy once, write `os-*` only). Pre-auth OS surface uses `data-surface="os"`; shell chrome naming no longer paints “root”.
