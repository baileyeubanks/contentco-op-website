# CURSOR-BLIND-⑤-CCO-OS — `/os` shell hygiene

**2026-08-03** — Purged dead `.root-*` OS shell class CSS (workspace/atlas/module/table residue renamed to `os-*` where still referenced; unused ROOT skin rules removed). Forced Inter on `body:has([data-surface="os"])` via root `--font-os` so Cream Plus Jakarta no longer bleeds under the Sapphire login shell. Legacy `/root` redirects unchanged.
