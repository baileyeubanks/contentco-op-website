# Non-Canonical Mirror

This monorepo app folder is not the canonical deploy source for public Co-Cut.

- Canonical local product repo: `/Users/baileyeubanks/Desktop/Projects/contentco-op/cocut`
- Canonical current Git remote / Coolify source: `git@github.com:baileyeubanks/coedit.git`
- Why this exists: legacy mirror and migration reference inside the monorepo
- Normal live workflow: push `contentco-op/cocut` `main` and let Coolify auto-deploy from `baileyeubanks/coedit`

Do not assume edits here will change the live public product unless the deploy
contract is intentionally rewritten and the repo identity is updated together.
